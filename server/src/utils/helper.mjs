import { Secret } from "../mongoose/schema/secretSchema.mjs";
import { User } from "../mongoose/schema/userSchema.mjs";

export const addSecretToUser = async (user, secret) => {
//   try {
//     const newSecret = await Secret.create(secret);
//     console.log("secret saved !!!");
//     console.log(newSecret);
//     user.secrets.push(newSecret._id);
//     const updatedUser = await User.save(user);
//     console.log(`1111111`);
//   } catch (error) {
//     console.log(`2222222`);
//     throw error;
//   }
};
